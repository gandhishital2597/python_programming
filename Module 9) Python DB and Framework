SESSION 1 - Introduction to Django &amp; SetupTopicsWhat is Django?Why Django vs FlaskDjango
featuresMVT ArchitectureInstalling DjangoCreating first Django projectRunning Django
serverTrainer ActivitiesCreate project: doctorfinder_projectExplore project structure
Task :
1. Install Django using pip in your local Python environment and verify the installation by running 'django-admin --version' in the terminal.
Answer : pip3 install django
python -m django --version
6.0.5
2.Create a new Django project named 'foodiehub_project' using the django-admin startproject command, then run the development server and open http://127.0.0.1:8000/ to confirm it's working.
Answer : django-admin startproject foodiehub_project
cd foodiehub_project
python manage.py runserver
http://127.0.0.1:8000/
3.Explore the folder structure of your 'foodiehub_project' and write down the purpose
of each key file: manage.py, settings.py, urls.py, wsgi.py, and asgi.py.
<br><br><em><strong>Hint:</strong> Check the official Django documentation or use ChatGPT to help you describe each file's role.</em>
Answer :  foodiehub_project/
│
├── manage.py
└── foodiehub_project/
    ├── __init__.py
    ├── settings.py
    ├── urls.py
    ├── asgi.py
    └── wsgi.py
Purpose of Each File
manage.py
Command-line utility for Django
Used to run server, migrations, create apps, etc.

Example:

python manage.py runserver
settings.py
Main configuration file of your project
Contains:
Installed apps
Database settings
Middleware
Static files config
urls.py
Defines URL routing
Connects URLs to views (logic)

Example:

path('admin/', admin.site.urls)
wsgi.py
Used for deploying Django with WSGI servers
Handles synchronous web requests
Used in production environments (e.g., Gunicorn)
asgi.py
Used for asynchronous support
Handles real-time apps (WebSockets, chat apps)
Works with ASGI servers like Daphne or Uvicorn
4. Write a short comparison (3-4 lines) between Django and Flask, focusing on features and use cases for each. Use an example app you use daily (like Instagram or Zomato) to explain which framework would be better and why.
Answer :
Django is a full-featured framework with built-in tools like authentication, admin panel, and ORM. It’s best for large applications.
Flask is lightweight and flexible, giving developers more control but requiring manual setup for many features.
Example:
Apps like Instagram or Zomato (large-scale, complex features) are better suited for Django because it provides built-in structure and scalability.
Flask is better for small apps, APIs, or microservices.
------------------------------------------------------------------------------------------
SESSION 2 - Apps, Settings & URL RoutingTopicsCreating Django appProject vs
Appsettings.py, urls.py, wsgi.pyMapping URL ? ViewHttpResponse basicsActivitiesCreate app:
 accountsAdd it to INSTALLED_APPSCreate first view and URL

1.Create a new Django app named 'playlists' inside your existing Django project and confirm that a playlists folder with the standard files (views.py, models.py, etc.) is generated.
ANswer :
python manage.py startapp playlists
playlists/
│
├── __init__.py
├── admin.py
├── apps.py
├── models.py
├── tests.py
├── views.py

2.Add 'playlists' to the INSTALLED_APPS list in your project's settings.py file so that Django recognizes it.
Answer : INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    ...
    'playlists',   # 👈 add this line
]
3. In playlists/views.py, write a function home(request) that returns an HttpResponse displaying 'Welcome to My Spotify Playlists!'.
Answer : from django.http import HttpResponse

def home(request):
    return HttpResponse("Welcome to My Spotify Playlists!")
4. Configure your project's urls.py to map the URL path 'music/' to the home view you created in playlists/views.py, so visiting http://localhost:8000/music/ shows your welcome message.
Answer :
from django.contrib import admin
from django.urls import path
from playlists.views import home   # 👈 import your view

urlpatterns = [
    path('admin/', admin.site.urls),
    path('music/', home),   # 👈 map URL
]
5. Change the welcome message in your playlists/views.py home view to include your name, then restart the server and verify
the update appears at http://localhost:8000/music/.<br><br><em><strong>Hint:</strong>
This helps you see how Django reloads views and how URL mapping works in real time.</em>
Answer : python manage.py runserver
http://127.0.0.1:8000/music/
Welcome to My Spotify Playlists!

views.py
from django.http import HttpResponse

def home(request):
    return HttpResponse("Welcome to My Spotify Playlists! - Shital Gandhi")
Welcome to My Spotify Playlists! - Shital Gandhi
-------------------------------------------------------------------------------------------------------

